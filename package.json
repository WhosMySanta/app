{
  "devDependencies": {
    "lerna": "2.0.0-beta.32"
  },
  "scripts": {
    "flow": "lerna run flow",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "publish": "lerna publish"
  }
}
