type Query {
  raffleGroup(id: String): RaffleGroup
}

type RaffleGroup {
  id: String
  title: String
  description: String
  suggestions: Suggestions
}

type Suggestions {
  currency: String
  minLimit: Int
  maxLimit: Int
}
