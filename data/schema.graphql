type Group {
  id: String
  title: String
  description: String
  suggestions: Suggestions
}

type Query {
  group(id: String): Group
}

type Suggestions {
  currency: String
  minLimit: Int
  maxLimit: Int
}
