schema {
  query: Query
  mutation: GroupMutations
}

type Group {
  id: String
  title: String
  description: String
}

input GroupInput {
  id: String
  title: String
  description: String
}

type GroupMutations {
  createGroup(input: GroupInput): Group
}

type Query {
  group(id: String): Group
}
