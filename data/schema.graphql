type Query {
  raffleGroup: RaffleGroup
}

type RaffleGroup {
  title: String
  description: String
  suggestions: Suggestions
}

type Suggestions {
  currency: String
  minLimit: Int
  maxLimit: Int
}
